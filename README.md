# 🚀 MasterDNS Toolkit

<img width="498" height="481" alt="image" src="https://github.com/user-attachments/assets/b8858f8f-fb60-49c5-9aad-561d89b27641" />


ابزاری برای استخراج، مرتب‌سازی و تست سرعت IPها در بستر **MasterDNS VPN** با قابلیت اتوماسیون کامل.


 ابزار پیداکردن ریزالور واقعی با تست برای پروژه https://github.com/masterking32/MasterDnsVPN

به پروزه ایشان بابت کارشون امتیاز بدید.
---

## 📌 معرفی

این اسکریپت برای انجام عملیات‌های زیر طراحی شده است:

* استخراج IP از فایل‌های مختلف
* مرتب‌سازی و حذف IPهای تکراری
* تست اتصال و سرعت هر IP از طریق SOCKS5
* مدیریت و آماده‌سازی فایل‌های تنظیمات
* تبدیل فایل‌های خروجی برای تحلیل بهتر

---

## ⚙️ پیش‌نیازها

قبل از اجرای اسکریپت، موارد زیر باید روی سیستم شما نصب باشد:

```bash
sudo apt install curl netcat bc
```

---

## 🛠 نصب و راه‌اندازی


حتما فایل ها را درون یک فولدر مجزا قرار دهید

```bash
mkdir /root/MDNS
```
فایل های اجرایی کلاینت خود به همراه تنظیمات کلاینت بروز شده را در درون فولدر قرار دهید.
فایل اجرایی MasterDNS را به BINMDV تغییر بدید که با فایل اجرایی قبلی خودتون قاتی نشه .

برای مثال من فایل MasterDnsVPN_Client_Linux_AMD64_v2026.04.07.233605-b5a4474 رو تغییر نام دادم.
```
mv MasterDnsVPN_Client_Linux_AMD64_v2026.04.07.233605-b5a4474 BINMDV
chmod +x BINMDV
```
### 1. دانلود اسکریپت

```bash
curl -O https://raw.githubusercontent.com/ExtremeDot/MasterDNS_Toolkit/main/MDNS_Toolkit.sh 
chmod +x MDNS_Toolkit.sh
```
###### اگر دسترسی ندارید میتوانید از طریق پورت ساکس که خود برنامه اجرا کرده دانلود کنید، ای پی و پورت ، یوزرنیم و پسورد خودتون رو جایگزین کنید

```bash
curl --socks5 127.0.0.1:10800 --proxy-user user:pass  -O https://raw.githubusercontent.com/ExtremeDot/MasterDNS_Toolkit/main/MDNS_Toolkit.sh && chmod +x MDNS_Toolkit.sh 
chmod +x MDNS_Toolkit.sh
```

---

### 2. تنظیم فایل config

در اولین اجرا، فایل زیر به‌صورت خودکار ساخته می‌شود:

```
MasterDNS_tool.cfg
```

در صورت نیاز آن را ویرایش کنید:

```bash
nano MasterDNS_tool.cfg
```

---

### ❗ نکته مهم

حتماً نام فایل اجرایی را مطابق سیستم خود تغییر دهید:

```bash
EXECUTABLE="./MasterDnsVPN_Client_Linux_AMD64_v2026.03.12.145701-00150d5"
```

🔺 این مقدار باید دقیقاً برابر با نام فایل واقعی کلاینت شما باشد.

---

## 🚦 قبل از اجرا

در صورتی که از یک پورت یکسان برای تست و Master DNS VPN استفاده میکنید ، سرویس MasterDNS باید متوقف شود:

```bash
sudo systemctl stop masterdnsvpn
```

---

## 📂 ساختار فایل‌ها

| فایل                     | توضیح               |
| ------------------------ | ------------------- |
| `sorted_ip.txt`          | لیست IPهای مرتب شده |
| `client_resolvers.txt`   | IP در حال تست       |
| `output/`                | خروجی‌ها و لاگ‌ها   |
| `client_config.toml`     | تنظیمات کلاینت      |
| `client_config.original` | بکاپ تنظیمات        |

---

## 🧠 نحوه عملکرد اسکریپت

### مرحله 1: استخراج IP

IPها از فایل‌های ورودی خوانده می‌شوند و فقط IPهای معتبر نگه داشته می‌شوند.

### مرحله 2: مرتب‌سازی

IPها به‌صورت عددی مرتب شده و تکراری‌ها حذف می‌شوند.

### مرحله 3: تست اتصال

برای هر IP:

1. اجرای کلاینت MasterDNS
2. بررسی فعال شدن SOCKS5
3. ارسال درخواست تست (3 بار)

### مرحله 4: تست سرعت

در صورت موفق بودن اتصال:

* دانلود تست از Cloudflare انجام می‌شود
* سرعت (KB/s) و زمان ثبت می‌شود

### مرحله 5: ثبت لاگ

تمام نتایج در فایل زیر ذخیره می‌شوند:

```
output/speed_DATE.log
```

---

## 🧪 دستورات (Commands)

### 🔹 استخراج IP از لاگ‌ها

به این صورت که از تمای لاگ های ساخته شده در اجرای های قبلی MasterDNS VPN ، آی پی های بدون تکرار را استخراج میکند و درون فایل sorted_ip.txt  قرار میدهد. 
در انتها برنامه از همین sorted_ip.txt استفاده کرده و تست اتصال واقعی گرفته و خروجی ریزالورهای تست شده را در فولدر output ذخیره میکند که درانتها میتوانید از آن برای client_resolvers.txt استفاده کنید.

```bash
./MDNS_Toolkit.sh sa
```

---

### 🔹 مرتب‌سازی IP از فایل

برای زمانی هست که میخوایم فقط از یک فایل خاص آی پی ها رو استخراج کنیم، برای یک لاگ خاص خروجی از برنامه. 

```bash
./MDNS_Toolkit.sh s file.txt
```

---

### 🔹 تست سرعت

این دستور تمامی ریزالورها از فایل sorted_ip.txt رو دونه به دونه توی فایل client_resolvers.txt قرار میده و بصورت تکی ازشون تست واقعی سرعت میگیرهف در صورتی که یکی از 3 تا تست ها موفق باشه ، اون ریزالور رو توی خروجی speed_log توی فولدر output میفرسته.

```bash
./MDNS_Toolkit.sh st
```

یا برای یک IP خاص:
برای زمانی هست که نمیخواید از فایل sorted_ip.txt استفاده کنید، با وارد کردن ریزالور ابتدا به فایل resolver اضافه میکنه و بعد ازش تست میگیره.
```bash
./MDNS_Toolkit.sh st 1.2.3.4
```

---

### 🔹 تبدیل فایل‌های txt به log

برای زمانی هست که فایل هایی که ای پی های ریزالور ما درون txt هستند استفاده میکنه و تمامی فایل های درون فولدر رو بجز client_resolver.txt رو به پسوند log تغییر میده، این قابلیت کاربردی برای زمانی هست که میخوایم تمامی آی پی ریزالورهای درون لاگ ها رو درون فایل sorted_ip.txt انتقال بدیم.

```bash
./MDNS_Toolkit.sh conv
```

📌 همه فایل‌های `.txt` به `.log` تبدیل می‌شوند به‌جز:

* `client_resolvers.txt`
* `sorted_ip.txt`

---

### 🔹 آماده‌سازی client_config

```bash
./MDNS_Toolkit.sh prep
```

✔ این دستور:

* از فایل بکاپ می‌گیرد
* مقادیر بهینه را اعمال می‌کند
تغییراتی که روی فایل config-client.toml اعمال میکند شامل موارد زیر می باشد:


```
SAVE_MTU_SERVERS_TO_FILE = false

MIN_UPLOAD_MTU = 40
MIN_DOWNLOAD_MTU = 500
MAX_UPLOAD_MTU = 120
MAX_DOWNLOAD_MTU = 900

LISTEN_IP = "127.0.0.1"
LISTEN_PORT = 55555
SOCKS5_AUTH = true
SOCKS5_USER = "ab1"
SOCKS5_PASS = "ab2"
---

### 🔹 همگام‌سازی تنظیمات

 
```bash
./MDNS_Toolkit.sh sync
```

✔ این دستور:

* اطلاعات SOCKS را از `client_config.toml` می‌خواند
* در `MasterDNS_tool.cfg` اعمال می‌کند

---

## 📊 خروجی‌ها

* فایل مرتب‌شده:

تمامی آی پی ریزالورهای استخراج شده در فایل sorted_ip.txt انتقال داده میشوند.
در زمان انجام تست، هر آی پی که تست گرفته میشود از این فایل حذف شده و اجرای تست تا زمانی که تمامی ای پی ریزالورها به اتمام برسند ادامه خواهد داشت.
```
sorted_ip.txt
```

* نسخه بکاپ با تاریخ:

```
output/sorted_ip_YYYY-MM-DD_HH-MM-SS.txt
```

* لاگ تست سرعت:

```
output/speed_YYYY-MM-DD_HH-MM-SS.log
```

---

## ⚠️ نکات مهم

* حتما تست را در یک فولدر جداگانه انجام دهید.
* فایل اجرایی MasterVPN را به BINMDV تغییر دهید.
* فایل `client_config.toml` باید وجود داشته باشد
* نام EXECUTABLE را حتماً اصلاح کنید ، باید مطابق نامی باشد که برای فایل اجرایی MasterDNs انتخاب میکنید.
* در صورت اجرا روی سرور، دسترسی‌های لازم بررسی شود

---

### مثال کاربردی

* ساخت فولدر
```bash
mkdir /root/mdns_tool
cd /root/mdns_tool
```

* کپی کردن فایل های مورد نیاز
فرض را بر این میگذاریم که فایل MasterDNS که به درستی کانفیگ شده و کار میده توی فولدر root هست
یه فولدر جدید میسازیم و فایل ها از root به اون انتقال میدیم.

```
cp /root/MasterDnsVPN_Client_Linux_AMD64_v2026.04.07.233605-b5a4474 /root/mdns_tool/MDV
cp /root/client_config.toml /root/mdns_tool/client_config.toml
cp /root/client_resolvers.txt /root/mdns_tool/client_resolvers.txt
chmod +x /root/mdns_tool/MDV
```
حالا توی فولدر mdns_tool فایل های زیر رو داریم
```
mdns_tool -
            - MDV
            - client_config.toml
            - client_resolvers.txt
```

* فایل اجرایی برنامه رو دانلود میکنیم، من روی پورت 10800 با یوزر 1234 و پسورد 5678 قبلا یه پراکسی با MasterDnsVPN_Client اجرا کردم که با همون فایل رو دانلود میکنم

```
cd /root/mdns_tool/
curl --socks5 127.0.0.1:10800 --proxy-user 1234:4321 -O https://raw.githubusercontent.com/ExtremeDot/MasterDNS_Toolkit/main/MDNS_Toolkit.sh 
chmod +x MDNS_Toolkit.sh
```
* فایل رو اجرا میکنم
```
./MDNS_Toolkit.sh
```

برنامه اجرا میشه و بصورت زیر نمایش داده میشه
```

███╗   ███╗██████╗ ███╗   ██╗███████╗
████╗ ████║██╔══██╗████╗  ██║██╔════╝
██╔████╔██║██║  ██║██╔██╗ ██║███████╗
██║╚██╔╝██║██║  ██║██║╚██╗██║╚════██║
██║ ╚═╝ ██║██████╔╝██║ ╚████║███████║
╚═╝     ╚═╝╚═════╝ ╚═╝  ╚═══╝╚══════╝
MasterDNS Toolkit

Sample config created: ./MasterDNS_tool.cfg
================ MasterDNS Toolkit =================
General Commands:
sa        → extract IPs from all logs
s file    → sort IPs from file
st [IP]   → speed test

Utility:
conv      → convert txt → log
prep      → prepare client_config.toml
sync      → sync cfg from client_config

Notes:
Stop service before run:
sudo systemctl stop masterdnsvpn
====================================================
```

* قدم بعدی جمع آوری فایل های لاگ هست که برنامه MasterDnsVPN_Client موقع اجرا ساخته و ریزالورهایی که کار میده و پیدا میکرده رو توی اونها ذخیره میکرده، برای اینکه نرم افزار لاگ گیری کنه باید توی client_config.toml مقدار زیر رو برابر با true قرار بدید.
```
SAVE_MTU_SERVERS_TO_FILE = true
```

حالا من لاگ های قبلی رو کپی میزنم و میارم به فولدر نرم افزار 
```
cp /root/run_test__* /root/mdns_tool/
```

حالا نرم افزار رو با فلگ sa اجرا میکنم تا آی پی ریزالورهای موجود در لاگ ها رو استخراج کنه و بفرسته توی sorted_ip.txt 
```
bash MDNS_Toolkit.sh sa
```

خروجی این شکلیه
```

███╗   ███╗██████╗ ███╗   ██╗███████╗
████╗ ████║██╔══██╗████╗  ██║██╔════╝
██╔████╔██║██║  ██║██╔██╗ ██║███████╗
██║╚██╔╝██║██║  ██║██║╚██╗██║╚════██║
██║ ╚═╝ ██║██████╔╝██║ ╚████║███████║
╚═╝     ╚═╝╚═════╝ ╚═╝  ╚═══╝╚══════╝
MasterDNS Toolkit

Sorted -> sorted_ip.txt
```

قدم بعدی اماده سازی کانفیگ هست
دستورات زیر رو به ترتیب وارد میکنیم:
```
 bash MDNS_Toolkit.sh prep
```

```
bash MDNS_Toolkit.sh sync
```
در انتها میتونید چک کنید که تنظیمات بدرستی اعمال شده باشند:
```
nano MasterDNS_tool.cfg
```

برای اینکه مطمین بشید تنظیمات بدون خطا هستند، یک بار فایل MDV را اجرا کنید.
```
./MDV
```
اگه برنامه بدرستی کار کنه در انتها پیغام بالا اومدن روی پروکسی رو بهتون نشون میده
```
[MasterDnsVPN Client] [INFO] 🚀 SOCKS5 Proxy server is listening on 127.0.0.1:5555
```
با کیلد های `ctrl + c` برنامه را ببندید:

با دستور زیر تست را شروع میکنیم

```
bash MDNS_Toolkit.sh st
```

برنامه شروع به گرفتن تست میکند



---


برای انکه بعد از خروج از ssh تست متوقف نشود میتوانیم از `tmux` استفاده کنیم

پنجره speedtest را باز میکنیم

```
tmux new -s speedtest
```

برای خروج موقت از پنجره tmux `ctrl+b` و سپس کلید `b` را فشار دهید.

برای بازگشت به پنجره tmux دستور زیر را تایپ کنید:
```
tmux attach -t speedtest
```
---
توضیحات کاملتر tmux

در صورتی که از طریق SSH به یک سرور متصل هستید، اگر اتصال شما قطع شود (مثلاً اینترنت شما موقتاً قطع شود یا از SSH خارج شوید)، تمام دستوراتی که در حال اجرا هستند نیز متوقف می‌شوند. برای جلوگیری از این مشکل، می‌توان از ابزار tmux استفاده کرد.

# آموزش استفاده از tmux برای جلوگیری از قطع شدن دستورات در SSH

اگر از طریق SSH به سرور متصل هستید، با قطع شدن اتصال (مثلاً قطع اینترنت یا بستن ترمینال)، تمام دستورات در حال اجرا متوقف می‌شوند.  
برای جلوگیری از این مشکل، می‌توانید از ابزار `tmux` استفاده کنید.

---

# آموزش استفاده از tmux برای جلوگیری از قطع شدن دستورات در SSH

اگر از طریق SSH به سرور متصل هستید، با قطع شدن اتصال (مثلاً قطع اینترنت یا بستن ترمینال)، تمام دستورات در حال اجرا متوقف می‌شوند.  
برای جلوگیری از این مشکل، می‌توانید از ابزار `tmux` استفاده کنید.

---

## tmux چیست؟

`tmux` یک ابزار مدیریت ترمینال (Terminal Multiplexer) است که به شما اجازه می‌دهد:

- چندین ترمینال داخل یک SSH داشته باشید
- حتی در صورت قطع شدن اتصال، برنامه‌ها همچنان اجرا شوند
- بعداً دوباره به همان محیط برگردید

---

## نصب tmux

اگر `tmux` نصب نیست، با یکی از دستورات زیر آن را نصب کنید:

### Ubuntu / Debian
```bash
sudo apt install tmux -y
```

ایجاد یک Session جدید

برای شروع کار، یک session جدید بسازید:

tmux new -s speedtest

بعد از اجرای این دستور، وارد محیط tmux می‌شوید.

اجرای دستور مورد نظر

حالا داخل tmux هر دستوری که می‌خواهید اجرا کنید. مثال:

speedtest
خروج موقت بدون توقف برنامه (Detach)

برای خروج از tmux بدون اینکه برنامه متوقف شود:

کلیدهای زیر را بزنید:
Ctrl + b
سپس:
d

با این کار از tmux خارج می‌شوید ولی برنامه همچنان در حال اجرا می‌ماند.

بازگشت به Session

برای بازگشت به همان session:

tmux attach -t speedtest
مشاهده لیست Sessionها
tmux ls
بستن Session
داخل tmux:
exit
از بیرون:
tmux kill-session -t speedtest
دستورات کاربردی
ایجاد پنجره جدید:
Ctrl + b سپس c
جابجایی بین پنجره‌ها:
Ctrl + b سپس n  (بعدی)
Ctrl + b سپس p  (قبلی)
جمع‌بندی

با استفاده از tmux:

قطع شدن SSH باعث توقف برنامه‌ها نمی‌شود
می‌توانید کارهای طولانی مثل speedtest، دانلود یا اجرای اسکریپت را بدون نگرانی انجام دهید
امکان مدیریت چندین محیط کاری به صورت همزمان دارید
